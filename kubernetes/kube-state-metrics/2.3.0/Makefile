# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=kubernetes
REPOSITORY?=kube-state-metrics
TAG?=2.3.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)

BINARY=kube-state-metrics

default: image

image: $(BINARY)
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

$(BINARY):
	curl -OL https://github.com/merore/kube-state-metrics/releases/download/v2.3.0/kube-state-metrics
	chmod +x $(BINARY)
