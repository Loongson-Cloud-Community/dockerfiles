# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=oliver006
REPOSITORY?=redis_exporter
TAG?=1.44.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


binary=redis_exporter

default: image

image: $(binary)
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

$(binary):
	wget -O redis_exporter https://github.com/Loongson-Cloud-Community/redis_exporter/releases/download/v1.44.0/redis_exporter-1.44.0-loong64
	chmod +x redis_exporter
