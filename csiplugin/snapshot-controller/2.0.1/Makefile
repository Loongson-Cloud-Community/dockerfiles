# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=csiplugin
REPOSITORY?=snapshot-controller
TAG?=2.0.1

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


default: image

download: clean
	wget "https://github.com/Loongson-Cloud-Community/kubernetes-csi_external-snapshotter/releases/download/v2.0.1/snapshot-controller"
	chmod 755 snapshot-controller

image: download
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

clean:
	rm -f snapshot-controller
