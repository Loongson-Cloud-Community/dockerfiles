# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=kbudde
REPOSITORY?=rabbitmq_exporter
TAG?=1.0.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)

BINARY=rabbitmq_exporter

default: image

image: $(BINARY)
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

$(BINARY):
	wget -O $(BINARY) https://github.com/Loongson-Cloud-Community/rabbitmq_exporter/releases/download/v1.0.0-RC19/rabbitmq_exporter-1.0.0-linux-loong64
	chmod +x $(BINARY)
