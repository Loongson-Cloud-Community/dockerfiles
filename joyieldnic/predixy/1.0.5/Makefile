# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=joyieldnic
REPOSITORY?=predixy
TAG?=1.0.5

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


default: image

download: clean
	wget -O predixy-$(TAG).zip "https://github.com/joyieldInc/predixy/archive/refs/tags/$(TAG).zip"

image: download
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

clean:
	rm -f predixy-$(TAG).zip
