# This file is generated by the template.

ORGANIZATION?=T_ORGANIZATION
REPOSITORY_NAME?=T_REPOSITORY
TAG?=T_TAG

IMAGE=$(OGRGANIZATION)/$(REPOSITORY):$(TAG)


default: container

container:
	docker build --no-cache \
		-t $(IMAGE) \
		.
