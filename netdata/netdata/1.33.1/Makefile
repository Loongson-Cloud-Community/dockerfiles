# This file is generated by the template.

REGISTRY	?=cr.loongnix.cn
ORGANIZATION	?=netdata
REPOSITORY	?=netdata
TAG		?=1.33.1
LATEST		?=false

RELEASE_URL=https://cloud.loongnix.cn/releases/loongarch64abi1/netdata/netdata/1.33.1/netdata-loongarch64-v1.33.1.tar.gz
IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest

default: image

release:
	wget -q https://cloud.loongnix.cn/releases/loongarch64abi1/netdata/netdata/1.33.1/netdata-loongarch64-v1.33.1.tar.gz

image: release
	docker build \
		-t $(IMAGE) \
                -f Dockerfile \
		.

push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
