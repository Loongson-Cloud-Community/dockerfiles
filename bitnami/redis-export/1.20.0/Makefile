# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=bitnami
REPOSITORY?=redis-export
TAG?=1.20.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


default: image

image:
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)
