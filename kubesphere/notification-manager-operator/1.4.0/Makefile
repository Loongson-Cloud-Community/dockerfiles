# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=kubesphere
REPOSITORY?=notification-manager-operator
TAG?=1.4.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)

BINARY=notification-manager-operator

default: image

image: $(BINARY)
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)


$(BINARY):
	curl -OL https://github.com/Loongson-Cloud-Community/notification-manager/releases/download/v1.4.0/notification-manager-operator
	chmod +x $(BINARY)
