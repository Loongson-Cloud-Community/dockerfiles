# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=kubesphere
REPOSITORY?=notification-manager
TAG?=1.4.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)

BINARY=notification-manager

default: image

image: $(BINARY)
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

$(BINARY):
	curl -OL https://github.com/Loongson-Cloud-Community/notification-manager/releases/download/v1.4.0/notification-manager
	chmod +x $(BINARY)
