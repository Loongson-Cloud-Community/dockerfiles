# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=kubesphere
REPOSITORY?=notification-tenant-sidecar
TAG?=3.2.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)

BINARY=tenant-sidecar

default: image

image: $(BINARY)
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

$(BINARY):
	curl -L -o $(BINARY) https://github.com/Loongson-Cloud-Community/notification-manager/releases/download/v1.4.0/tenant-sidecar-3.2.0-linux-loong64
	chmod +x $(BINARY)
