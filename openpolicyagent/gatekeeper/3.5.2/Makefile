# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=openpolicyagent
REPOSITORY?=gatekeeper
TAG?=3.5.2

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


BINARY=manager

default: image

image: $(BINARY)
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

$(BINARY):
	wget https://github.com/Loongson-Cloud-Community/gatekeeper/releases/download/v3.5.2/manager
	chmod +x manager
