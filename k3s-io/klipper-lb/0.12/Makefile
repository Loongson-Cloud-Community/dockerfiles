# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=k3s-io
REPOSITORY?=klipper-lb
TAG?=0.12

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


default: image

image:
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)
