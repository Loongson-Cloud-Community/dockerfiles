# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=library
REPOSITORY?=golang
TAG?=1.19-buster

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


ALIAS=cr.loongnix.cn/library/golang:1.19
default: image

image:
	docker build \
		-t $(IMAGE) \
		.

push: alias
	docker push $(IMAGE)
	docker push $(ALIAS)


alias:
	docker tag $(IMAGE) $(ALIAS)
