# This file is generated by the template.

REGISTRY      ?= cr.loongnix.cn
ORGANIZATION  ?= library
REPOSITORY    ?= golang

GOMAJOR       ?= 1.24
GOMINOR       ?= 13
OSVERSION     ?= buster
TAG           ?= $(GOMAJOR)-$(OSVERSION)

IMAGE   = $(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
ALIAS0  = $(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(GOMAJOR)
ALIAS1  = $(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(GOMAJOR).$(GOMINOR)-$(OSVERSION)
ALIAS2  = $(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(GOMAJOR).$(GOMINOR)

LATEST =

default: image

image:
	docker build -t $(IMAGE) .

push: alias
	docker push $(IMAGE)
	docker push $(ALIAS0)
	docker push $(ALIAS1)
	docker push $(ALIAS2)

ifneq ($(LATEST),)
	docker tag $(IMAGE) $(LATEST)
	docker push $(LATEST)
endif

alias:
	docker tag $(IMAGE) $(ALIAS0)
	docker tag $(IMAGE) $(ALIAS1)
	docker tag $(IMAGE) $(ALIAS2)
