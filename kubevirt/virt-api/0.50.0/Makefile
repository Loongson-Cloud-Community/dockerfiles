# This file is generated by the template.

REGISTRY?=cr.loongnix.cn
ORGANIZATION?=kubevirt
REPOSITORY?=virt-api
TAG?=0.50.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


default: image

download:
	wget "https://github.com/Loongson-Cloud-Community/kubevirt/releases/download/v0.50.0/virt-api"
	chmod +x virt-api

image: download
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

clean:
	rm -f virt-api
